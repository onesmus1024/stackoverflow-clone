import { QuestionShortenerPipe } from './question-shortener.pipe';

describe('QuestionShortenerPipe', () => {
  it('create an instance', () => {
    const pipe = new QuestionShortenerPipe();
    expect(pipe).toBeTruthy();
  });
});
